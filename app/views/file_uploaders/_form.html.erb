<%= form_for @file_uploader, :html => {:multipart => true} do |f| %>
  <% if @file_uploader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@file_uploader.errors.count, "error") %> prohibited this file_uploader from being saved:</h2>

      <ul>
      <% @file_uploader.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.file_field :image %>
    <%= f.submit %>
  </div>
<% end %>
